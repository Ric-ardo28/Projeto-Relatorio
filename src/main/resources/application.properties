# Dados de conexão com o banco PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/relatoriotest
spring.datasource.username=postgres
spring.datasource.password=helo2912
spring.jpa.hibernate.ddl-auto=create



# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configurações específicas do H2 (desnecessárias no PostgreSQL) foram removidas
